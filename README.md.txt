this code takes a text file with calculations and after performing the calculation it uses the result to index to a different line where
it will repeat this until it returns to the same line
the text doc must be correctly formatted: goto calc (operator) (Number) (Number)